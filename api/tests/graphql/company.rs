use crate::graphql::test_graphql;

#[tokio::test]
async fn test_get_company() {
    test_graphql(|filter, context| async move {
        // Arrange.

        // Act.

        // Assert.
        assert!(false)
    })
    .await
}
